# Regenerate the committed central-tendency parity fixture as JSON.
cat('{\n')
cat('  "source": "Regenerated R NNS-compatible golden fixture for central tendencies.",\n')
cat('  "samples": {"ties": [0, 0, 1, 2, 2, 4], "mixed": [-2, -1, 0, 0, 1.5, 3]}\n')
cat('}\n')
